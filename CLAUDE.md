# jzon

## Development workflow

- Always use the `/tdd` skill when developing. Every feature follows the RED→GREEN→REFACTOR cycle, one test at a time.
- Use `/tiger-style` and `/tiger-style-architecture` for design discipline: bounded resources, comptime validation, assertion vs error separation, explicit state machines.
